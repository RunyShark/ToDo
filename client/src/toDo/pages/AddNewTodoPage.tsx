import { Typography } from "@mui/material";
import { TodoView, UserLayout } from "../../index";

export const AddNewTodoPage = () => {
  return (
    <UserLayout>
      <TodoView />
    </UserLayout>
  );
};
